{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "attach",
            "address": "localhost",
            "port": 9222,
            "name": "Attach Chrome",
            "url": "http://localhost:3000/",
            "webRoot": "${workspaceFolder}"
          }
    ]
}